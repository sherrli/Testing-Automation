# print(base64.b64decode(x))
# need to convert x to bytes
# for all --- in json
import base64
import json
import os

# for all sites...
# username = browser.find_element_by_name("SID")
# assert not None
# clear the username field
# username.send_keys((base64.b64decode(pw)).decode())
